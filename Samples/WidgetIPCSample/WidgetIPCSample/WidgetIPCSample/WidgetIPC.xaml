<Page
    x:Class="WidgetIPCSample.WidgetIPC"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:WidgetIPCSample"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d">

    <StackPanel Orientation="Vertical" HorizontalAlignment="Center" VerticalAlignment="Center">
        <TextBlock Text="WidgetIPC" Padding="0,0,0,5"/>
       <Button x:Name="myButton" Click="MyButton_Click">Click Me</Button>
    </StackPanel>

  <!--<TextBox x:Name="ShortcutTextBox"
                   x:Uid="ShortcutTextBox"
                   Height="32"
                   MinWidth="150"
                   VerticalAlignment="Top"
                   AcceptsReturn="False"
                   AutomationProperties.LabeledBy="{Binding ElementName=ShortcutLabel}"
                   BorderBrush="{ThemeResource TextBoxBorderBrush}"
                   BorderThickness="1"
                    />-->
</Page>
